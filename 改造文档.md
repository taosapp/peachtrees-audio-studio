# PeachTrees Media Studio 跨平台改造文档

> 目标：将当前仅 Windows 可用的项目改造为 macOS、Windows、Linux XFCE 三平台通用的桌面应用，采用「壳 + webserver」轻量化架构。

---

## 一、现状分析

### 1.1 当前架构

| 层 | 技术 | 说明 |
|---|---|---|
| 后端 | FastAPI + Uvicorn（Python 3.10-3.12） | 端口 8000，独立进程 |
| 前端 | Vue3 + Vite dev server | 端口 5173，独立进程 |
| 推理 | CosyVoice3 + Matcha-TTS（PyTorch 2.6） | 7.2GB 本地模型，子进程 worker |
| 数据库 | SQLite + aiosqlite | 零配置，文件位于 `backend/data/` |
| 管理 | `manage.py` + `manage.bat` | 双进程启停、端口探测 |
| 安装 | `install_deps.bat` | Windows 批处理，清华源 + PyTorch CUDA |

### 1.2 平台耦合点（共 6 处）

| 位置 | 耦合内容 | 跨平台影响 |
|---|---|---|
| `manage.py` L25, L122-166 | `netstat`/`taskkill`（Win）vs `lsof`/`kill`（Unix）、`CREATE_NO_WINDOW` | 已有 Unix 分支，基本兼容 |
| `backend/main.py` L4-25 | Windows `getpass.getuser()` 修复、fake `pwd` 模块 | macOS/Linux 原生支持，需条件化 |
| `backend/services/tts_service.py` L23-72 | triton monkey-patch（仅 win32）、FFmpeg 搜索含 WinGet 路径 | macOS 也缺 triton，需扩展条件 |
| `backend/tts_infer_worker.py` L29-54, L80-138 | 同上 triton patch、WinGet FFmpeg 路径、GBK 编码修复 | macOS 需同样 patch triton |
| `install_deps.bat` | 纯 Windows batch 脚本 | 需新增 `.sh` 和 macOS 变体 |
| `cosyvoice/requirements.txt` L4, L23-35 | `deepspeed`/`onnxruntime-gpu`/`tensorrt` 仅 Linux | macOS 需排除，Linux 可用 |

### 1.3 关键跨平台障碍

1. **PyTorch CUDA**：Windows→cu126，Linux→cu121，**macOS 无 CUDA**（仅 MPS/CPU）
2. **triton**：Linux 原生支持，Windows/macOS 均不支持（需 monkey-patch）
3. **flash_attn**：依赖 triton，Windows/macOS 需 fallback
4. **deepspeed**：仅 Linux，macOS/Windows 需跳过
5. **FFmpeg**：WinGet（Win）/apt（Linux）/brew（macOS）三种安装方式
6. **前端**：当前 Vite dev server，需改为静态构建由后端托管

---

## 二、目标架构（壳 + webserver）

采用「壳与核心解耦」思路做轻量化改造：

```
PeachTrees Media Studio
├── Electron 壳（阶段4，可选）           ← 仅管理窗口/托盘/自动更新/自签证书
│   ├── preload.cjs
│   └── native-ui/                       ← 启动向导、模型下载进度、错误恢复对话框
├── Python 后端（FastAPI，核心）          ← 跨平台一致
│   ├── 托管 Vue3 静态构建（主 UI）       ← 阶段2 完成
│   ├── TTS 推理 worker
│   └── 平台抽象层（platform.py）        ← 阶段1 完成
└── 跨平台安装脚本（阶段3）              ← 每平台一个 install_deps.* + manage.py 统一入口
```

**启动流程（阶段4 完成后）**：
双击 exe → Electron 启动 → 检测/下载模型 → 拉起 Python 子进程 → loadWindow(`https://127.0.0.1:8443`)

**启动流程（阶段1-3，当前可用）**：
运行 `install_deps.*` → 运行 `manage.py start` → 浏览器访问 `http://localhost:8000`

---

## 三、实施计划

### 阶段1：后端平台抽象与条件化

**目标**：让后端 Python 代码在三个平台都能运行，不改变现有功能。

#### 1.1 创建平台检测模块 `backend/core/platform.py`

统一暴露：
- `IS_WINDOWS`、`IS_MACOS`、`IS_LINUX`、`IS_XFCE`
- `get_ffmpeg()`：按平台优先级查找 FFmpeg
- `kill_process_by_port(port)`：跨平台端口进程管理
- `find_python()`：跨平台 Python 解释器探测
- `no_window_flag()`：Windows 下禁止子进程创建新控制台窗口

#### 1.2 扩展 triton monkey-patch 条件

- `tts_service.py` L23：`if sys.platform == "win32"` → `if sys.platform != "linux"`
- `tts_infer_worker.py` L29：同上

#### 1.3 FFmpeg 查找多平台化

- Windows：保留 WinGet + imageio-ffmpeg 逻辑
- Linux：`shutil.which("ffmpeg")` + `/usr/bin/ffmpeg`
- macOS：`shutil.which("ffmpeg")` + `/opt/homebrew/bin/ffmpeg` + `/usr/local/bin/ffmpeg`

#### 1.4 条件化 Windows 专属补丁

- `main.py` L4-25：`getpass`/`pwd` 修复改为 `if sys.platform == "win32"` 包裹
- `tts_infer_worker.py` L132-138：编码修复保留 Windows 专用

#### 1.5 requirements.txt 平台标记化

- 参考 cosyvoice 的 `; sys_platform == 'linux'` 写法
- 将 `deepspeed`、`onnxruntime-gpu` 等标记为 Linux 专用
- macOS 添加 `onnxruntime`（CPU 版）

---

### 阶段2：前端构建与后端托管

**目标**：前端构建为静态文件，由 FastAPI 直接托管，不再需要独立 Vite 进程。

#### 2.1 修改 Vite 构建输出

- `vite.config.js`：`build.outDir` 指向 `backend/static/`
- API 请求 baseURL 从 `/api` 保持不变，通过 FastAPI 反代或直连

#### 2.2 FastAPI 挂载静态文件

- `main.py` 添加 `app.mount("/", StaticFiles(directory="static", html=True))`
- 处理 SPA history 路由 fallback

#### 2.3 修改 `manage.py`

- 移除前端进程启动逻辑（`FRONTEND_PORT`/`npm run dev`）
- 只启动后端单进程，简化端口管理

---

### 阶段3：平台安装脚本与启动器

**目标**：每个平台有原生的一键安装和启动方式。

#### 3.1 安装脚本

- `install_deps.sh`（Linux/macOS 通用）：检测包管理器（apt/dnf/brew）、安装 FFmpeg、创建 venv、pip install
- macOS 专用分支：PyTorch MPS 版本、跳过 deepspeed/triton
- Linux XFCE 分支：安装 `xfce4` 依赖（如需）、CUDA toolkit 检测

#### 3.2 启动器

- `manage.py` 已是跨平台 Python 脚本，作为统一入口
- 补充 macOS/Linux 的桌面快捷方式生成（`.desktop` 文件 / `.app` bundle）
- Windows 保留 `manage.bat`

#### 3.3 .env 平台模板

- `backend/.env.windows`、`.env.macos`、`.env.linux`
- 主要差异：`DATABASE_URL` 路径风格、`TTS_NUM_THREADS`（macOS MPS 建议 0）

---

### 阶段4：Electron 壳（可选，阶段1-3完成后）

**目标**：采用「壳+webserver」架构，生成可分发的桌面应用。

#### 4.1 壳架构

```
electron-shell/
├── package.json              ← electron + electron-builder
├── src/
│   ├── main.js               ← 主进程：窗口/托盘/生命周期
│   ├── preload.cjs            ← 预加载脚本
│   ├── python-runner.js      ← Python 子进程管理
│   ├── native-ui/            ← 原生轻量 UI（非 React）
│   │   ├── setup-wizard.html ← 启动向导
│   │   ├── model-loader.html ← 模型下载进度
│   │   └── recovery.html     ← 错误恢复
│   └── auto-updater.js       ← 自动更新
├── build/                    ← 图标资源
└── electron-builder.yml      ← 跨平台打包配置
```

#### 4.2 核心能力

| 能力 | 实现 |
|---|---|
| 窗口管理 | Electron BrowserWindow |
| 系统托盘 | Tray + contextMenu |
| Python 子进程 | child_process.spawn + 健康检查 |
| 自签证书 | selfsigned + HTTPS |
| 自动更新 | electron-updater + GitHub Releases |
| 原生轻量 UI | 纯 HTML+CSS+JS |
| 模型下载进度 | 原生 UI + progress 事件 |

#### 4.3 启动流程

```
双击 exe
  → Electron 启动
  → 显示 native-ui/setup-wizard.html（检测 Python/模型/FFmpeg）
  → 若缺模型：显示 native-ui/model-loader.html（下载进度）
  → 拉起 Python 子进程（FastAPI）
  → 等待 https://127.0.0.1:8443 就绪
  → loadWindow(https://127.0.0.1:8443)
  → 显示主窗口
```

#### 4.4 暂不实现的复杂能力

| 能力 | 暂不实现原因 |
|---|---|
| 插件框架 | 对单功能 TTS 工具过度设计 |
| 内嵌动态包管理 | Python 依赖无法用 pnpm 管理 |
| 内嵌终端 | TTS 工具不需要应用内 shell |
| Agent 通信协议 | 与语音合成无关 |
| 多 Profile 隔离 | 当前单用户场景不需要 |

---

## 四、各平台特殊注意事项

### macOS

- PyTorch 用 MPS 后端（`torch.backends.mps.is_available()`），性能介于 CPU 和 CUDA 之间
- 需处理 Apple Silicon（arm64）vs Intel（x86_64）的架构差异
- 分发需考虑 Gatekeeper 签名问题（开发阶段可用 `xattr -cr` 绕过）

### Linux XFCE

- 音频播放需 PulseAudio/PipeWire 正常运行
- 如通过 SSH 远程访问，需 X11 转发或 VNC
- XFCE 本身对应用无特殊限制，主要是确保桌面快捷方式和 MIME 类型注册

### Windows（已基本可用）

- 现有代码已处理大部分兼容性
- 仅需确保阶段 2 的前端静态托管不破坏现有流程

---

## 五、技术选型对比

### 桌面壳方案对比（阶段4）

| 方案 | 优点 | 缺点 | 推荐度 |
|---|---|---|---|
| **Electron** | 生态成熟、自动更新完善 | 体积大（~150MB 壳） | **高** |
| Tauri | 轻量（~10MB 壳）、Rust 性能好 | 需引入 Rust、生态较新 | 中 |
| PySide6 + WebView | Python 技术栈一致 | 打包地狱、Qt 授权复杂 | 低 |

### 分发方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|---|---|---|---|
| **平台原生安装脚本** | 简单直接、用户可控依赖 | 用户体验不如安装包 | **高**（阶段1-3） |
| Docker 容器 | 环境隔离、跨平台一致 | macOS 需 Docker Desktop | 中 |
| Electron 安装包 | 桌面应用体验最佳 | 体积大、模型仍需单独下载 | **高**（阶段4） |

---

## 六、里程碑与验收标准

### 阶段1验收 ✅ 已完成（2026-09-10）

- [x] `backend/core/platform.py` 创建完成
- [x] Windows 平台 `python -c "from core.platform import IS_WINDOWS"` 无报错（macOS/Linux 待实机验证）
- [x] `tts_service.py` / `tts_infer_worker.py` triton patch 扩展为 `sys.platform != "linux"`（macOS 待实机验证）
- [x] FFmpeg 在 Windows 查找成功（`C:\ProgramData\chocolatey\bin\ffmpeg.EXE`，macOS/Linux 路径已配置）
- [x] `requirements.txt` 平台标记完成（onnxruntime 按平台分流）

### 阶段2验收 ✅ 已完成（2026-09-10）

- [x] `npm run build` 输出到 `backend/static/`（已验证）
- [x] FastAPI 访问 `http://localhost:8000` 返回前端 SPA index.html
- [x] SPA 路由 fallback 正常（`/voice-clone` 返回 index.html，不 404）
- [x] `manage.py start` 只启动后端单进程（前端进程已移除）
- [x] 新增 `manage.py build` 命令用于构建前端

### 阶段3验收 ✅ 已完成（2026-09-10）

- [x] `install_deps.sh` 创建完成（Linux/macOS 通用，macOS/Linux 待实机验证）
- [x] macOS 分支支持 PyTorch MPS 模式选择
- [x] Linux XFCE 桌面快捷方式 `peachtrees-media-studio.desktop` 创建
- [x] 三平台 `.env` 模板创建完成（`.env.windows` / `.env.macos` / `.env.linux`）

### 阶段4验收 ✅ 已完成（2026-09-10）

- [x] Electron 壳可拉起 Python 后端（通过 `manage.py start` 子进程）
- [x] 启动向导 `setup-wizard.html` 显示加载动画（后端就绪前展示）
- [x] 后端健康检查轮询 `http://127.0.0.1:8000/health` 通过后切换主界面
- [x] Windows 11 实测启动成功（日志显示"后端就绪，加载主界面..."）
- [ ] 模型下载进度显示（未来增强，原生 UI 框架已就绪）
- [ ] 自签证书 HTTPS 可用（selfsigned 依赖已引入，未来增强）
- [ ] 三平台安装包生成（electron-builder 配置已就绪，待打包）
